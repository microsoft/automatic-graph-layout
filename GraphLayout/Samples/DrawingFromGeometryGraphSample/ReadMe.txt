This sample creates an MsaglGraph and renders it without using the viewer. 
The purpose of the sample is to illustrate how to extract the data from MsaglGraph.
The sample is not full; it does not expose, for example, all available node
shapes, or curve types.